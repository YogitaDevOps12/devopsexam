
apiVersion: v1
kind: Service
metadata:
  name: grafana
  namespace: devsecops
spec:
  type: LoadBalancer
  selector:
    app.kubernetes.io/instance: kube-prometheus-stack
    app.kubernetes.io/name: grafana
  ports:
    - port: 3000
      targetPort: 3000
